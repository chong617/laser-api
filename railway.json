{
  "build": {
    "env": {
      "PYTHONUNBUFFERED": "1"
    },
    "buildCommand": "pip install -r requirements.txt && python -m laserembeddings download-models",
    "startCommand": "python app.py"
  }
}
